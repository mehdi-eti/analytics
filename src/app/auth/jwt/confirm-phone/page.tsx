// sections
import { JWTConfirmPhoneView } from '@/src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: confirm phone',
};

export default function RegisterPage() {
  return <JWTConfirmPhoneView />;
}
